// Placeholder until runpod/deploy-template.mjs is run with a RUNPOD_API_KEY. That script
// overwrites this file with the real deployUrl. Until then the site falls back to repoUrl.
window.BLOCKBUSTER = {
  deployUrl: '',
  repoUrl: 'https://github.com/OWNER/blockbuster-studio',
};
